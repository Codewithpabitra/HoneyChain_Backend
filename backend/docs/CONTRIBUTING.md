main            → always deployable, protected
dev             → integration branch, merge features here first
feature/<name>  → e.g. feature/hive-crud, feature/google-auth
fix/<name>      → e.g. fix/rate-limit-bug